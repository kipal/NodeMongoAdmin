module.exports = {
    frontend : {
        web : {
            port : 8080,
            host : "localhost"
        }
    },
    api : {
        mongodb : {
            port : 3000,
            host : "localhost"
        }
    }
};